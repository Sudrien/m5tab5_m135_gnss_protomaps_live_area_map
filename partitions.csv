# Partition table, copied verbatim from the Arduino core's
# tools/partitions/app3M_fat9M_16MB.csv - the scheme named in the
# PartitionScheme=app3M_fat9M_16MB build flag used by arduino-cli.
#
# It has to match exactly, or the two builds produce different flash layouts
# and switching between them would invalidate whatever the other one wrote:
# nvs holds the wifi credential store, and app0/app1 must sit where the
# bootloader expects.
#
# Name,   Type, SubType, Offset,  Size, Flags
nvs,      data, nvs,     0x9000,  0x5000,
otadata,  data, ota,     0xe000,  0x2000,
app0,     app,  ota_0,   0x10000, 0x300000,
app1,     app,  ota_1,   0x310000,0x300000,
ffat,     data, fat,     0x610000,0x9E0000,
coredump, data, coredump,0xFF0000,0x10000,
